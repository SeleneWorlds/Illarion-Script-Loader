MessageDialog.SeleneConstructor = function(title, message, callback)
    return {
        type = "MessageDialog",
        title = title,
        message = message,
        callback = callback
    }
end