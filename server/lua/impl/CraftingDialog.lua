CraftingDialog.SeleneConstructor = function(title, sfx, sfxDuration, callback)
    return {
        type = "CraftingDialog",
        title = title,
        sfx = sfx,
        sfxDuration = sfxDuration,
        callback = callback
    }
end
